import './app-shell/app-shell.js';

/**
 * main.js bootstraps the application: imports and registers components, mounts the root component in the DOM, initializes the router and global services
 */

// Mount the root AppShell
document.body.innerHTML = `<app-shell></app-shell>`;